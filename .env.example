# Core networking
ORCHESTRATOR_PORT=8000
REDIS_URL=redis://redis:6379/0
ORCHESTRATOR_BASE_URL=http://orchestrator:8000

# Authentication & logging
ORCHESTRATOR_TOKEN=dev-token
LOG_LEVEL=INFO

# Persistence paths inside containers
ORCHESTRATOR_RESULTS_DIR=/data/orchestrator/results
ORCHESTRATOR_STATE_DIR=/data/orchestrator/state
RESULTS_DIR=/data/worker/results

# Worker configuration
TASK_TOPIC=tasks

# (Optional) extra environment for validation scripts
# ORCHESTRATOR_URL=http://127.0.0.1:8000
